# Generated Stage-Risk Adaptive v1 Configurations

This directory is reserved for frozen, generated configs used by the formal
bank, controller, dev, full, and transfer runs. The committed template is
copied here by the preparation script before each frozen run, together with its
checkpoint path and hashes. Do not place generated YAML files under `runs/`.
