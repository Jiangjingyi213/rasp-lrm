seed: 3

workflow:
  profile: pilot
  root: runs/08_stage_calibrated_pruning/main_{profile}_llm_pruner_mlp_qwen3_1p7b_t20_full

stages: [setup, reasoning, verify, final]

model:
  name_or_path: Qwen/Qwen3-1.7B
  dtype: float32
  device_map: auto
  trust_remote_code: true
  attn_implementation: eager

prompt:
  ordinary:
    use_chat_template: true
    enable_thinking: true
    answer_format: boxed
  structured:
    use_chat_template: true
    enable_thinking: true
    explicit_stage_protocol: true
    forced_assistant_prefix: "[[STAGE_SETUP]]\n"

generation:
  max_input_tokens: 4096
  max_new_tokens: 4096
  temperature: 0.6
  top_p: 0.95
  top_k: 20

calibration_pool:
  allowed_sources: ["*"]
  excluded_sources: [GSM8k, GSM8K, MATH, Omni-MATH, amc_aime, cn_k12]

masks:
  metric: WIFV
  structure: UL-UM
  ratios: [0.00]
  c4_samples: 0
  bias_compensation: true

llm_pruner_mlp_static_width:
  enabled: true
  importance: l2
  structure: UL-UM
  physical_pruning: true
  variants:
    - method_name: llm_pruner_mlp_t20_matched
      prune_ratio: 0.23
      target_pruning_ratio: 0.23
      target_pruning_label: t20
      matched_rasp_reference: t20_math_safe
      selection_note: "LLM-Pruner-style static width baseline for Qwen3-1.7B; prunes matched MLP intermediate channels using L2 magnitude, no LoRA/recovery, aligned to the observed t20 MLP pruning level."

evaluation:
  math500_name_or_path: HuggingFaceH4/MATH-500
  allow_final_without_dev: true
  final_datasets:
    - dataset: gsm8k
      split: test
    - dataset: math500
      name_or_path: HuggingFaceH4/MATH-500
      split: test

profiles:
  smoke:
    final_eval_limit: 16
    final_seeds: [3]
    reuse_existing_method_summaries: true
    final_methods:
      - llm_pruner_mlp_t20_matched

  pilot:
    final_eval_limit: -1
    final_seeds: [3]
    reuse_existing_method_summaries: true
    final_methods:
      - llm_pruner_mlp_t20_matched
